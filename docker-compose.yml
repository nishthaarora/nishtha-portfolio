services:
  app:
    image: ghcr.io/nishthaarora/nishtha-portfolio:${IMAGE_TAG:-latest}
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
